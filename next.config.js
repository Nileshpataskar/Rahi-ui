/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "picsum.photos",
      "api.dicebear.com",
      "tiimg.tistatic.com",
      "drive.google.com",
    ],
  },
};

module.exports = nextConfig;
