/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "picsum.photos", "api.dicebear.com","tiimg.tistatic.com"],
  },
};

module.exports = nextConfig;
